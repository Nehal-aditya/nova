-- hello_universe.nv
-- Simplest NOVA program: print a greeting to verify Phase 0 lexer/parser

mission main() → Void {
  transmit("Hello, universe!");
}

-- Test comments work
-- And basic expressions
mission basic_math() → Int {
  let x = 5;
  let y = 3;
  return x + y;
}
